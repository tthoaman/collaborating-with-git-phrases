# Phrases

## Work items

1. [x] Create a `greet()` function that prints "Hello, World!".
2. [x] Create a `chat()` function that prints "How are you?".
3. [x] Create a `farewell()` function that prints "Goodbye, World!".